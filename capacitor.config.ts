import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.danagbriel.subsmngr',
  appName: 'subsMngr',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
