import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.dangabriel.subsmngr',
  appName: 'subsMngr',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
